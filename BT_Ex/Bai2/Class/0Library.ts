export class Library {
    code: number;
    publisher: string;
    copy: number;

    constructor(code: number, publisher: string, copy: number) {
        this.code = code;
        this.publisher = publisher;
        this.copy = copy;
    }
    
}
